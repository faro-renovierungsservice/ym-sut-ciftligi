name: YM Süt Çiftliği Sitesini Yayınla

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Site dosyalarını al
        uses: actions/checkout@v4

      - name: GitHub Pages ayarları
        uses: actions/configure-pages@v5

      - name: Siteyi hazırla
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Siteyi yayınla
        id: deployment
        uses: actions/deploy-pages@v4
